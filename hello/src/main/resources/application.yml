server:
  port: 8801

#设置服务名
spring:
  application:
    name: hello-service


#设置注册中心地址
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8888/eureka


